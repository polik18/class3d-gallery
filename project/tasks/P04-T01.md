# P04-T01：IndexedDB 本機作品庫

提供選配保存、容量預檢、版本遷移、匯出、匯入與清除。所有資料只存在同一瀏覽器。

## 已完成

- 使用 IndexedDB 保存作品資料與原始二進位檔，schema 明確標記為 v1。
- 寫入與整批還原前先檢查瀏覽器可用容量，保留 10% 安全餘裕。
- 支援單件讀取、列出、刪除與完整清除；作品不會上傳伺服器。
- 自訂二進位展覽封裝可匯出、解析並匯回完整作品庫，不使用 Base64 膨脹檔案。
- Chrome 實測跨頁面重新載入仍可讀回作品，清除、匯入與二進位內容比對均通過。
- 全專案 36 項測試通過；證據見 `project/evidence/P04-T01/verification.json`。
